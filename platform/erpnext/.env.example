# ERPNext Configuration
# Set these in Coolify's service environment variables

# ERPNext version (Docker tag)
ERPNEXT_VERSION=v15

# Site name — must match your domain. Live ERPNext is admin.orengen.io;
# app.orengen.io is reserved for the future client-login CRM and must NOT
# be set as the Frappe site name on this bench.
SITE_NAME=admin.orengen.io

# MariaDB root password (generate a strong one)
DB_ROOT_PASSWORD=change-me-strong-password

# ERPNext admin password (for Administrator user)
ADMIN_PASSWORD=change-me-admin-password
