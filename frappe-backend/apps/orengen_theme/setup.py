from setuptools import setup, find_packages

setup(
      name="orengen_theme",
      version="1.0.0",
      description="Custom branding theme for OrenGen ERP",
      author="OrenGen",
      author_email="support@orengen.io",
      packages=find_packages(),
      zip_safe=False,
      include_package_data=True,
      install_requires=[],
)
